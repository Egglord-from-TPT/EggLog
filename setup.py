from setuptools import setup, find_packages

setup(
    name="egg",
    version="1.0.0",
    packages=find_packages(),
    description="A python library that logs all output.",
    author="",
    python_requires=">=3.7",
)
